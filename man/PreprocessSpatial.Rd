% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCP-spatial-workflow.R
\name{PreprocessSpatial}
\alias{PreprocessSpatial}
\title{Preprocess Spatial Transcriptomics Data}
\usage{
PreprocessSpatial(
  srt,
  normalization_method = "SCTransform",
  nfeatures = 3000,
  npcs = 30,
  dims = NULL,
  resolution = 0.8,
  run_umap = TRUE,
  run_clustering = TRUE,
  assay = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{Seurat object with spatial data}

\item{normalization_method}{Normalization method: "SCTransform" (recommended) or "LogNormalize"}

\item{nfeatures}{Number of variable features (used if normalization_method="LogNormalize")}

\item{npcs}{Number of principal components to compute}

\item{dims}{PCs to use for UMAP and clustering (default: 1:npcs)}

\item{resolution}{Clustering resolution (default: 0.8)}

\item{run_umap}{Whether to run UMAP (default: TRUE)}

\item{run_clustering}{Whether to run clustering (default: TRUE)}

\item{assay}{Assay to use (default: DefaultAssay)}

\item{verbose}{Print progress messages}

\item{...}{Additional parameters passed to normalization functions}
}
\value{
Preprocessed Seurat object
}
\description{
Standard preprocessing pipeline for spatial transcriptomics, including
normalization, feature selection, scaling, dimensionality reduction, and clustering.
Uses SCTransform by default (recommended for spatial data).
}
\examples{
\dontrun{
srt <- LoadSpatialData("path/to/data")
srt <- PreprocessSpatial(srt)

# Custom settings
srt <- PreprocessSpatial(
  srt,
  normalization_method = "LogNormalize",
  nfeatures = 5000,
  resolution = 0.5
)
}

}
