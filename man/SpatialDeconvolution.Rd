% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SCP-analysis.R
\name{SpatialDeconvolution}
\alias{SpatialDeconvolution}
\title{Spatial Deconvolution with scRNA-seq Reference}
\usage{
SpatialDeconvolution(
  srt,
  reference,
  cell_type_col = "cell_type",
  normalization.method = "SCT",
  dims = 1:30,
  k.filter = 200,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{srt}{Spatial Seurat object}

\item{reference}{scRNA-seq reference Seurat object with cell type annotations}

\item{cell_type_col}{Column name in reference metadata containing cell type labels}

\item{normalization.method}{Normalization method (default: "SCT" for SCTransform)}

\item{dims}{Dimensions to use for integration (default: 1:30)}

\item{k.filter}{Number of neighbors for filtering (default: 200)}

\item{verbose}{Print progress messages}

\item{...}{Additional parameters passed to FindTransferAnchors}
}
\value{
Spatial Seurat object with predicted cell type proportions
}
\description{
Performs cell type deconvolution on spatial transcriptomics data using
a single-cell RNA-seq reference. Uses Seurat's integration/transfer methods.
}
\examples{
\dontrun{
# Load spatial and reference data
srt_spatial <- LoadSpatialData("path/to/spatial")
srt_spatial <- PreprocessSpatial(srt_spatial)

# Reference with cell type annotations
srt_ref <- readRDS("path/to/reference.rds")

# Run deconvolution
srt_spatial <- SpatialDeconvolution(
  srt_spatial,
  reference = srt_ref,
  cell_type_col = "cell_type"
)

# View predictions
head(srt_spatial@meta.data[, grep("predicted", colnames(srt_spatial@meta.data))])
}

}
